"""Reusable pipeline stages for the BVD branching process calibration and analysis

Each stage is a module exposing ``main(...)``. End-to-end orchestration that runs these in order
lives in the ``workflows`` package (e.g. ``workflows.detection_range``), not here.
"""
